import { render } from '@testing-library/react';

import LogonScreen from './logon-screen';

describe('LogonScreen', () => {
  // it('should be able to render the logon screen', () => {
  //   const {} = render(<LogonScreen />);
  // });
});
